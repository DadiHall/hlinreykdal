<% @cart.product_items.each do |item| %>

	<table class="table">
		<tr>
			<th>Quantity</th>
			<th>Name</th>
			<th>Price &euro;</th>
			<th>Price ISK</th>
			<th>Image</th>

		</tr>
		<tr>
			<th><%= item.quantity %>&times; </th>
			<th><%= item.product.title %></th>
			<th><%= number_to_currency(item.total_price_usd, :unit => '€ ', :precision => 0)%></th>
			<th><%= number_to_currency(item.total_price_isl.to_i, :unit => "ISK ", :precision => 0) %></th>
			<th><%= image_tag item.product.image.url(:thumb) %></th>
		</tr>

	</table>
	
<%end %>